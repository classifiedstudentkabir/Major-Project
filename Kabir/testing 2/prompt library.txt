🎨 PHASE 2 — CSS SYSTEM & DESIGN TOKENS
Prompt name: 02_build_css_system.prompt
Based on the reference analysis:

1. Create a CSS design system:
   - colors (as CSS variables)
   - font scale
   - spacing scale
   - button styles
   - card styles

2. Create these files:
   /src/styles/variables.css
   /src/styles/base.css
   /src/styles/layout.css
   /src/styles/components.css
   /src/styles/responsive.css

Rules:
- Mobile-first
- Use Flexbox + Grid
- Comment each section clearly
- Match reference spacing & proportions closely

DO NOT build pages yet.


👉 After output, send me:

CSS file list

Any doubts AG mentions

🧱 PHASE 3 — PROJECT FILE STRUCTURE
Prompt name: 03_project_structure.prompt
Create the full project folder structure inside /src.

Include:
- pages/
- components/
- styles/
- js/
- lang/
- assets/images/

DO NOT write page content.
DO NOT write CSS again.
Only scaffold files with comments explaining purpose.

🌍 PHASE 4 — LANGUAGE SYSTEM (VERY IMPORTANT)
Prompt name: 04_language_system.prompt
Implement a JSON-based language system.

Requirements:
1. Languages: English (en), Hindi (hi), Marathi (mr)
2. Files:
   /src/lang/en.json
   /src/lang/hi.json
   /src/lang/mr.json
3. Create language switch UI:
   - Very visible
   - Simple buttons or dropdown
4. Language change must be instant (JS)
5. Default language = English
6. Comment JS clearly

DO NOT translate long content.
Use sample placeholder text only.


👉 After this, we will later replace text safely.

🏠 PHASE 5 — BUILD PAGES (ONE BY ONE)
Prompt name: 05_build_pages.prompt

Use this prompt separately for EACH page
(example for Home):

Build ONLY the Home page.

Reference:
- /references/home*.png

Rules:
1. Use existing CSS system
2. Add clear HTML comments per section
3. Use placeholder text
4. Images = placeholders with correct aspect ratio
5. Match layout & spacing closely
6. Fully responsive

DO NOT build other pages.


Repeat for:

About

Services

Blog

Contact

FAQ

Single pages

👉 After each page, send me:

Screenshot (if possible)

Or AG notes

⚙️ PHASE 6 — INTERACTIONS
Prompt name: 06_interactions.prompt
Add JavaScript interactions:

- Navbar toggle (mobile)
- FAQ accordion
- Sliders (if present)
- Counters (if present)

Rules:
- No heavy libraries
- JS must be readable & commented
- No visual redesign

📩 PHASE 7 — FIREBASE (FORMS ONLY)
Prompt name: 07_firebase_forms.prompt
Connect Firebase ONLY for contact/inquiry forms.

Requirements:
1. Use Firebase Firestore or Realtime DB
2. Store:
   - Name
   - Phone
   - Message
   - Timestamp
3. No authentication
4. Add basic validation
5. Comment code clearly

Explain setup steps briefly.

🖼️ PHASE 8 — IMAGE SWAP (FROM /design)
Prompt name: 08_image_swap.prompt
Replace placeholder images with real images from /design.

Rules:
1. Do NOT change layout or CSS
2. Match image purpose (hero, card, gallery)
3. Optimize file usage
4. Keep aspect ratio intact

🚀 PHASE 9 — DEPLOY
Prompt name: 09_deploy.prompt
Prepare project for free deployment.

1. GitHub Pages setup
2. Folder cleanup
3. Firebase config safety
4. Final checklist

DO NOT change UI.