# Firestore Rules Deployment Note

If you are not using Firebase CLI, copy the contents of `firestore.rules` and paste them into Firebase Console:
Firestore Database → Rules → Publish.

If you do use Firebase CLI, run:
`firebase deploy --only firestore:rules`
